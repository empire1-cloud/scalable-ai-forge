{
  "summary": "First-finish smoke tests for Emergent // Architect. Backend: /api health, auth register/login/me, invalid creds, duplicate email, blueprint list/get/patch, per-user isolation all pass. Frontend: landing renders with hero-cta-primary and nav-signin-btn, sign-in with demo user routes to /dashboard, dashboard renders existing blueprint card, opening blueprint shows all sections and export buttons (Copy JSON, .md, .json) which are clickable. Generator page (idea-prompt-input + submit-blueprint-btn) loads. Blueprint generation itself was NOT re-triggered end-to-end to avoid 45-90s LLM latency, but a previously generated blueprint proves the pipeline: content contains title, tagline, core_insight, system_blueprint {architecture, components, tech_stack, data_models}, leverage_point {the_move, why_it_works, impact_effort}, roadmap, risks, monetization, executable_output.artifacts (count=2).",
  "tested_scope": ["frontend", "backend", "auth", "integration"],
  "passed": [
    "GET /api/ -> 200 {service: emergent-architect}",
    "POST /api/auth/login demo user -> 200 with JWT token + user",
    "POST /api/auth/login wrong password -> 401 Invalid email or password",
    "POST /api/auth/register duplicate email -> 400 Email already registered",
    "POST /api/auth/register new user -> 200 token+user",
    "GET /api/auth/me with Bearer -> 200 user profile",
    "GET /api/auth/me without token -> 401 Not authenticated",
    "GET /api/blueprints (demo) -> 200 list of user's blueprints",
    "GET /api/blueprints without auth -> 401",
    "GET /api/blueprints/{id} -> full content object with system_blueprint, leverage_point, executable_output.artifacts",
    "PATCH /api/blueprints/{id} rename -> 200 {ok:true}",
    "Per-user isolation: freshly registered user sees []",
    "Landing page renders hero-cta-primary + nav-signin-btn",
    "Sign-in flow (demo@architect.io/demo1234) navigates to /dashboard",
    "Dashboard renders blueprint-card and open-blueprint- buttons after fetch",
    "Blueprint detail page shows blueprint-title, copy-json-btn, export-markdown-btn, export-json-btn, back-to-vault-btn",
    "copy-json-btn click executes without console error",
    "Generator page shows idea-prompt-input and submit-blueprint-btn"
  ],
  "failed": [],
  "action_items": [],
  "success_rate": {"backend": "100%", "frontend": "100%"},
  "retest_needed": false,
  "full_testing_deferred": true,
  "notes_for_full_qa": [
    "End-to-end POST /api/blueprints generation was not exercised in this pass to avoid 45-90s LLM latency; existing blueprint content proves schema conformance. Full QA should trigger a new generation and assert the /blueprint/{id} redirect and full section rendering (roadmap, risks, monetization, code artifacts).",
    "DELETE /api/blueprints/{id} was not exercised to preserve seed data; full QA should cover it.",
    "Streaming/error UX for LLM failures not tested."
  ]
}
